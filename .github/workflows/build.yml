name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install nuitka pyside6 playwright pycryptodome pillow ordered-set zstandard openpyxl requests
        playwright install chromium
    
    - name: Build with Nuitka
      run: |
        python -m nuitka ^
          --standalone ^
          --windows-console-mode=attach ^
          --enable-plugin=pyside6 ^
          --assume-yes-for-downloads ^
          GUI.py
      shell: cmd
    
    - name: Prepare release files
      run: |
        rem standaloneビルドの出力フォルダからコピー
        xcopy /E /I GUI.dist release
        
        rem exeの名前を変更
        ren release\GUI.exe ProjectWIN.exe
        
        rem _internalフォルダを作成（botsはサーバーからDLするので不要）
        mkdir release\_internal
        mkdir release\_internal\settings
        mkdir release\_internal\cookies
        mkdir release\_internal\cookies\Amazon
        mkdir release\_internal\cookies\iCloud
        
        rem version.jsonを作成
        echo {"version": "${{ github.ref_name }}"} > release\_internal\version.json
      shell: cmd
    
    - name: Create ZIP
      run: |
        Compress-Archive -Path release\* -DestinationPath ProjectWIN-${{ github.ref_name }}.zip
      shell: powershell
    
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: ProjectWIN-${{ github.ref_name }}
        path: ProjectWIN-${{ github.ref_name }}.zip
    
    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: ProjectWIN-${{ github.ref_name }}.zip
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
